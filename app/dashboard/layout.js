export const metadata = {
    title: "Dashboard - Easy Page Builder",
    description: "Manage your website projects",
    robots: {
        index: false,
        follow: false,
    },
};

export default function DashboardLayout({ children }) {
    return children;
}
