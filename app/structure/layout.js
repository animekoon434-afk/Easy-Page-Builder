export const metadata = {
    title: "Project Structure - Easy Page Builder",
    description: "Review and create your project structure",
    robots: {
        index: false,
        follow: false,
    },
};

export default function StructureLayout({ children }) {
    return children;
}
